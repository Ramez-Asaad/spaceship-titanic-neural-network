# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Notebooks
.ipynb_checkpoints/

# Environments / tooling
.env
.pytest_cache/
.ruff_cache/
.mypy_cache/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local agent config
.claude/

# Generated predictions
submission*.csv

# LaTeX build artifacts. The .tex source and the built .pdf are both tracked.
*.aux
*.fdb_latexmk
*.fls
*.log
*.out
*.bbl
*.blg
*.synctex.gz
*.toc

# Leftovers from the 2024 coursework that OneDrive cannot rehydrate on this
# machine ("cloud operation was unsuccessful"), so they cannot be committed.
# The report's outlier plot is reproducible from src/data.py; the predictions
# are reproducible with `python -m src.predict`.
outliers.png
predictions.csv

# The original coursework report is kept locally but not published. The
# technical write-up in reports/technical-report.pdf supersedes it.
reports/original-report.pdf

# NOTE: models/ is intentionally tracked. The trained weights are small (~280 KB)
# and the Streamlit demo needs them at runtime to deploy without a training step.
